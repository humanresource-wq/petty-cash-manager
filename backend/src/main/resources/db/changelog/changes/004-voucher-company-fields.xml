<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="004-voucher-company-fields" author="pettycash">
        <!-- Add columns initially as nullable -->
        <addColumn tableName="transactions">
            <column name="voucher_number" type="varchar(100)"/>
            <column name="company" type="varchar(100)"/>
        </addColumn>

        <!-- Backfill existing rows (if any) -->
        <update tableName="transactions">
            <column name="voucher_number" valueComputed="transaction_no"/>
            <column name="company" value="Freestone Infotech LLP"/>
        </update>

        <!-- Apply not-null constraints -->
        <addNotNullConstraint tableName="transactions" columnName="voucher_number" columnDataType="varchar(100)"/>
        <addNotNullConstraint tableName="transactions" columnName="company" columnDataType="varchar(100)"/>

        <!-- Add unique constraint on voucher number + date -->
        <addUniqueConstraint tableName="transactions" columnNames="voucher_number, date" constraintName="uq_transaction_voucher_date"/>
    </changeSet>

</databaseChangeLog>
